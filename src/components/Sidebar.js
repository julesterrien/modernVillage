import React from 'react';

import './App.css';

const Sidebar = () => (
  <nav className="menu">
    <div className="menu-item">Schedule</div>
    <div className="menu-item">Inbox</div>
    <div className="menu-item">Planner</div>
  </nav>
);

export default Sidebar;
